#include <stdio.h>
#include <conio.h>

main()
{       int rev=0;
	int rem,n;
	clrscr();
			printf("Enter Any Numbr :");
			scanf("%d",&n);
		 while(n!=0)
		 {
			rem = n%10;
			rev = rev * 10 + rem;

			n = n/10;

		 }
			printf("%d",rev);

	getch();
}