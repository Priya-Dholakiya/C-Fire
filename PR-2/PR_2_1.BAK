#include <stdio.h>
#include <conio.h>

main()
{
      int s;
      char grade;
      clrscr();

      printf("Enter your score(1 to 100):");
      scanf("%d",&s);

     grade = (s>=90)?'A':
      (s>=80)?'B':
      (s>=70)?'C':
      (s>=60)?'D':
      (s>=50)?'E':'F';
      (s<=33)?Sorry!! You are fail....;

      printf("Your grade is: %c\n",grade);

	getch();
}

